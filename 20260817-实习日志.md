# 修改完善上周内容
### 数据清洗代码存在的问题和修改
>1. 问题:df["Date"] = pd.to_datetime(df["Date"],format="mixed", errors="coerce")
>df = df.dropna(subset=["Date"])两行代码的错误使用
##### 第一行代码的作用是将不同格式的时日期表示全都转换成统一标准的pandas的日期表示格式datetime64,并将转换失败的(如2026-2-30，abc,8月23日等其他格式)变成NaT
##### 第二行代码的作用是查找Date列为空(NaT)的行删除这一整行
##### 两行代码结合起来就是智能转换Date格式，转换不成功的标记为空，再将所有Date列为空的行删除
##### 这两行代码使得许多转换失败的日期被全部删除，造成数据的大量缺失
##### 原来的Dataframe有71行，经过这两行代码变成48行，少了23行
2. 修改:
    >1. 复制原始文本，新增一列【备份原始日期字符串】
    >df["Date_original"] = df["Date"]
    >2. 原地覆盖 Date 列，转为标准 datetime；解析失败 → NaT
    >df["Date"] = pd.to_datetime(df["Date"], format="mixed", errors="coerce")
    >3. 新增布尔标记列，标识本行日期是否解析成功
    >df ["is_date_valid"] = df ["Date"].notna ()
##### 修改思路：创建两列新的Series一个用来保存修改之前的Date，一个用来判断哪些Date转换成功，哪些失败，这样不会轻易删除数据了
##### 代码重新运行结果是有66行（删除了完全空白和完全重复的Series）
### 整理 python 目录
1. 所有代码文件补上 .py 后缀
>原因：.py = Python 脚本专属标识，解释器靠这个后缀认出这是能执行的 Python 代码。
2. 删掉重复的 python/python/ 目录
3. 数据文件（csv、xlsx、json）和代码分开放，建个 data 目录
### 用pandas重做第14和19题
##### 14题用groupby
##### 19题用astype(str)和str.replace()和to_numeric()