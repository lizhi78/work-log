# 第二周总结
### 1. 这周学会了用pandas清洗数据的大致流程：
1. 删除完全空白数据和完全重复行
2. 处理缺失值(填充 fillna () / 删除 dropna ())
3. 处理脏数据有（将脏数据转换成统一格式的数据，方便后续处理）
4. 处理异常数据（删除或用"异常"代替）
5. 所有统计、groupby 汇总分析完成
### 2.本周强化了python代码的运用：包括字典和列表的相关知识，以及json与python的相互转换等
### 3.将代码封装到同一个文件夹中，在Github上不再是大段的代码，不够美观
1. `./` 向下进入子文件夹
2. `../` 向上退到上一级文件夹
3. 使用r进行转义
### 4. 学会了简单的调用API（但是对python要求挺高的）
### 5. 大概知道了对数据操作的顺序：
1. 使用SQL获取数据 
2. 2.使用pandas对数据进行处理和清洗 
3. 3.使用API对数据进行分析
### 不足
#### 对python和pandas的掌握远远不够，太过于欠缺，缺少实战，只能够勉强的把AI的代码看懂，有一些都看不懂，而且可能过一会就忘了，但还是有不少收获的。太过欠缺代码的积累和实战了，这让我在打代码时举步维艰。