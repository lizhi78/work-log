#  项目开头
- ### 新建一个独立的项目仓库（别再放 work-log 里了）
- ### 搭骨架：目录结构、.gitignore、requirements.txt、README 起个头
- ### 需求文档里有建议的文件划分
# loader.py部分
- ### 前提准备：1.学习Pathlib中的Path功能：
| 功能 | 代码示例 | 说明 |
|------|----------|------|
| 路径拼接 | `p = Path("./data") / "test.csv"` | 跨系统兼容，不用手动写斜杠 |
| 判断是否存在 | `p.exists()` | 文件/文件夹存在返回 True |
| 判断是否为文件 | `p.is_file()` | 判断是不是 csv、xlsx 这类文件 |
| 获取文件后缀 | `p.suffix.lower()` | 获取后缀并转小写，用于判断文件类型 |
| 获取文件名 | `p.name` | 获取文件名，如 `test.csv` |
| 创建文件夹 | `p.mkdir(exist_ok=True)` | 文件夹已存在不会抛出错误 |



### 2.re(regular expression)库的基础知识和准备（为明天的日期的格式统一做准备）
|函数|作用|返回结果|
|---|---|---|
|`re.findall(pattern, text)`|找出**全部**匹配的内容|列表，存放所有匹配到的字符串|
|`re.search(pattern, text)`|从文本找**第一个**匹配项|匹配成功返回Match对象；找不到返回None|
|`re.fullmatch(pattern, text)`|**整个字符串**完全和模式对上才匹配|全部匹配返回Match对象；否则None|
|`re.sub(pattern, repl, text)`|查找并替换匹配到的内容|返回替换完成后的新字符串|

## re.match / re.search / re.fullmatch

| 函数 | 匹配规则 | 成功返回 | 失败返回 | 获取匹配内容 | 适合场景 |
|---|---|---|---|---|---|
| `re.match(pattern,text)` | **只从字符串开头**匹配，后面字符不限制 | Match对象 | `None` | `.group()` 获取匹配内容<br>`.group(1)` 获取括号捕获组 | 校验字符串**开头**是否符合规则 |
| `re.search(pattern,text)` | **整个字符串任意位置搜索**，找到第一个就停止 | Match对象 | `None` | `.group()` 获取匹配内容<br>`.group(1)` 获取括号捕获组 | 提取文本中间内容，脏数据清洗首选 |
| `re.fullmatch(pattern,text)` | **整个字符串必须全部完全匹配**，不能多不能少 | Match对象 | `None` | `.group()` 获取匹配内容<br>`.group(1)` 获取括号捕获组 | 完整格式校验：手机号、纯数字校验 |


|类别|正则符号|含义|
|---|---|---|
|字符类别|`\d`|匹配1个数字|
||`\D`|匹配1个非数字|
||`\s`|匹配1个空格|
||`\S`|匹配1个非空格|
||`[abc]`|匹配a/b/c中任意一个字符|
||`[^abc]`|匹配除a/b/c以外任意字符|
||`.`|匹配除换行`\n`以外任意1个字符|
|量词（重复）|`*`|0个或多个|
||`+`|1个或多个|
||`?`|0个或1个|
||`{2}`|正好2次|
||`{2,5}`|2‑5次|
||`{2,}`|至少2次|
|组合模式|`\|`|或，二选一匹配|
||`()`|分组，可对分组使用量词|


### 3.loader.py部分（读取.csv文件和.xls和.xlsx文件的具体代码）
1. [读取代码](.loader.py)#github上使用
2. [读取代码](../data-report-tool/loader.py)#VS code使用


