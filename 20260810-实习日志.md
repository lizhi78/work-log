# 上周工作查缺补漏
1. 把上周所有代码从 .md 文件里搬出来，建成真正的 .py 文件
2. 修改并运行上周代码
> 1. Windows 记事本默认是 GBK 编码
> (需要用with open (path2,"r",encoding="gbk") as f:打开)
> 2. 变量命名要清晰，不要让一个变量承担多个含义
> 3.  except Exception as e:
>            print(f"出现错误:{e}")可以具体给出错误原因
# python练习题
1. [python_practice_1](./python/python练习1)
2. [python_practice_2](./python/python练习2)
3. [python_practice_3](./python/python练习3)
4. [python_practice_4](./python/python练习4)
>for k in 字典：只循环键（单个值）
>for k, v in 字典.items()：同时取出键 + 值（一对 2 个值，适配key,value双变量）
>.count()是方法：查重复次数;len()：查整体长度
>对象.方法() → 点 . 调用：属于这个数据自己自带的功能（方法）
>函数(对象) → 直接括号调用：独立公共函数，把数据丢进去使用
>round(数值, 保留位数)
>lambda用来创建没有函数名的临时小函数
>语法格式：lambda 参数列表: 返回表达式
>list.sort不生成全新list，而是对原list进行排序
>sorted(对象)返回全新list
>使用key=一个函数可以转变排序规则