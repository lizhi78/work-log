# pandas上手1
1. 装 pandas（pip install pandas openpyxl）
2. 读取CSV文件和Excel文件
>pandas.read_csv(path)和pandas.read_excel(path)
3. Series是一维列；DataFrame是多个Series组成的二维表格。一个Dataframe的一列是一个Series，一个Dataframe的一行是一个Series
>只要用 pandas 对应的读取函数读取表格类文本 / 表格文件，全部返回DataFrame，不只是 CSV。
>不同后缀对应不同读取方法，结果统一是 DataFrame。
4. 看数据：head() 看前几行、info() 看有哪些列和类型、describe() 看统计概况
5. 选某几列、按条件筛行、排序
>[pandas.py](../Desktop/python/pandas.py)
