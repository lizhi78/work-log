# 数据清洗 + 清洗报告(最关键)
1. 删除空白值：`df = df.dropna(how="all")` 将所有整行都为空的行删除
2. 删除重复行：`df = df.drop_duplicates(subset=df.columns, keep="first")` 将全部字段重复的行删除，并保留第一行
3. 日期解析：分为三种：1.`8月23日`  2.`2026‑2‑30`  3.`2026/2/30`
    - 通过正则`re.fullmatch`区分三种格式；"8月23日"补全年份；斜杠格式替换replace(r"\/", "-")为横杠
    - `pd.to_datetime(errors="coerce")`转为日期类型
    - 一开始的空值标记`未知`，非法日期标记`日期格式错误`。**判断依据：一开始是空值，转换成日期类型后还是空值类型的就标记成`未知`，一开始不是控制，转换成日期类型后不是数字类型的就标记成`日期格式错误`**

4. 数量、单价数值清洗
    - `.str.extract(r'(\d+)')`提取数字，剥离单位；`pd.to_numeric`转数字
    - `数量和单价(元)`缺失、提取失败全部`fillna(0)`填充0

5. F3清洗报告
    - 创建字典report保存数据处理报告 **(使用字典是因为可以键与值匹配)**
    - 每一步操作**之前记录行数/空值数量**，to_numeric或to_datetime转换后空值的部分减去转换前的空值部分得到的是转换失败的数量
    - report字典保存原始行数、每一步处理条数、字段缺失、格式错误统计
    - 输出报告：原始行数 → 每一步处理数量及原因 → 最终行数；记录每个字段缺失情况
### 在日期解析的过程中大量使用了有关正则表达式的知识

1. [清洗代码](.cleaner.py)#github上使用
2. [清洗代码](../data-report-tool/cleaner.py)#VS code使用